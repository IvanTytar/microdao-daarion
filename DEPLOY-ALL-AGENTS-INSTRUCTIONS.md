# 🚀 Інструкція: Деплой всіх агентів на НОДА2

## Швидкий спосіб (через браузер)

1. Відкрийте сторінку кабінета НОДА2: `http://localhost:8899/nodes/node-2`
2. Відкрийте консоль браузера (F12 → Console)
3. Скопіюйте та вставте код з файлу `scripts/deploy-all-node2-agents-browser.js`
4. Натисніть Enter

Скрипт автоматично задеплоїть всіх 50 агентів на НОДА2.

---

## Альтернативний спосіб (через UI)

1. Відкрийте сторінку кабінета НОДА2: `http://localhost:8899/nodes/node-2`
2. Перейдіть на вкладку "Агенти"
3. Натисніть кнопку **"Деплой всіх"** вгорі
4. Підтвердіть деплой

---

## Через кнопку в UI (рекомендовано)

На сторінці `/nodes/node-2` в розділі "Агенти" є кнопка **"Деплой всіх"**, яка автоматично задеплоїть всіх агентів.

---

## Статус деплою

Після деплою:
- Статус агентів оновлюється автоматично
- Зелені індикатори = задеплоєно та працює
- Жовті індикатори = задеплоєно, але статус невідомий
- Червоний індикатор = не задеплоєно або помилка

---

**Всього агентів на НОДА2: 50**

